{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "mergeConfidence:all-badges",
    "config:best-practices",
    "helpers:githubDigestChangelogs" // Fetch changelogs for git-refs updates
  ],
  "customManagers": [
    {
      "customType": "regex",
      "datasourceTemplate": "git-refs",
      "managerFilePatterns": [
        "/^nvim-pack-lock\\.json$/"
      ],
      "matchStrings": [
        '"(?<depName>[^"]+)": {[^}]*?"rev": "(?<currentDigest>[^"]+)"[^}]*?"src": "(?<packageName>[^"]+)",[^}]*?"version": "\'(?<currentValue>[^\'"]+)\'"'
      ],
      "versioningTemplate": "exact"
    }
  ]
}
